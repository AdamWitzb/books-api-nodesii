{
  "engines": {
    "node": "^14"
  },
  "name": "books-api",
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/app.js",
    "start:dev": "ts-node-dev --debug --respawn src/app.ts",
    "docker:debug:run": "docker-compose up --build",
    "docker:prod:build": "docker build -f docker/Dockerfile ."
  },
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "@types/express": "4.17.11",
    "ts-node-dev": "1.1.0",
    "typescript": "4.1.3"
  }
}
